    - 1️⃣ What is the difference between var, let, and const?
      --> The value of var and let can be changed but the value of const cant be.

    - 2️⃣ What is the spread operator (...)?
    --> it is is used to expand  elements of arrays and objects into individual elements.

    - 3️⃣ What is the difference between map(), filter(), and forEach()?
     --> map() transforms each element and returns a new array
         filter() returns a new array with matching elements
         forEach() just loops through elements and we can control the elemnts as we want..

    - 4️⃣ What is an arrow function?
      --> An arrow function is a showrter to write a function or a method.

    - 5️⃣ What are template literals?
     --> Template literals is a function in JavaScript written with backticks (`) that allow write htmnl codes or js or css using ${} .
